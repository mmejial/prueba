import { render } from "@testing-library/react"
import Title from "../add/Title"
import FirstApp from "../components/FirstApp"


describe('pruebas en title.jsx',()=>{
    test('no se que tiene que hacer',()=>{
        render(<Title/>)
        
    })
})